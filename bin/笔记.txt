项目要求
三层架构


视图层 键盘录入  打印结果  Menu 菜单 用户和计算机的交互的位置

业务逻辑层
注册: 查询注册账号是否已经存在?  如果不存在则调用增加功能
学生添加: 判断这个学生是否存在，然后再决定是否调用
数据访问层的增加功能

数据访问层      获取文本中所有的用户信息    传入一个id获取这个
id在文本中对应的完整用户信息  这一层只能有最简单的增，删，改，查功能
不提供能不能添加这个书法，里面的增加方法只要调用了，数据肯定是保存的

